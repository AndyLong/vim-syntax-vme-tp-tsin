" My TP Terminal Simulator Input File FileType Plugin file
"
" Only do this if the buffer has not been initialised
"
if exists("b:did_ftplugin")
	finish
endif

let b:did_ftpulgin=1

setlocal softtabstop=4
setlocal tabstop=4
setlocal expandtab
setlocal shiftwidth=4

